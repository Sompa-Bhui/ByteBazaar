Milestone 1 completed: scaffold created.

Next steps:
- Install dependencies with `pnpm install` or `npm install`.
- Copy `.env.example` to `.env` and provide secrets.
- Run `pnpm prisma generate` and `pnpm prisma migrate dev --name init`.
