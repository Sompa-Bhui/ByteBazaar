import BuildLabClient from '@/components/BuildLabClient';

export const metadata = {
  title: 'Build Lab | ByteBazaar',
  description: 'Plan, connect, and validate developer-tech setups with the ByteBazaar Build Lab.',
};

export default function BuildLabPage() {
  return <BuildLabClient />;
}
